program teste5corrigido
   %Este e um programa com if
        integer j, k, result;
        real a, jl;
{
read(j);
read(k);
     if (k != 0) then
        result := j/k;
     else
        result := 0;
end;
write(result); 
}