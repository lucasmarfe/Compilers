program teste5corrigido
   %Este e um programa com if
        integer j, k;
        real a, j;
{
read(j);
read(k);
     if (k != 0) then
        result := j/k;
     else
        result := 0;
end;
write(res); 
}