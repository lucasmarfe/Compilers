program teste4corrigido
    integer i, j, k, total, soma1, a;
  {
      i := 4 * (5-3 * 50 / 10);
      j := i * 10;
      k := i* j / k;
      k := 4 + a;
      write(i);
      write(j);
      write(k);
}