program teste3corrigido
    integer cont, qtd;
    real media, altura, soma;
   {
      cont := 5;
      soma := 0.0;
repeat
        write("Altura: ");
        read (altura);
        cont := soma;
        cont := cont - 1;
      until(cont+qtd>media);
      write("Media: ");
      write (soma / qtd);
}