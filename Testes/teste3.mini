program teste3
    integer cont;
    real media, altura, soma;
   {
      cont := 5;
      soma := 0.0;
repeat {
        write(“Altura: ”);
        read (altura);
        soma := soma # altura;
        cont := cont – 1;
      }
      upto(cont);
      write(“Media: ”);
      write (soma / qtd);
}