program teste6corrigido
   integer a, c, maior;
   real b;
   {
      read(a);
      read(b);
      read(c);
if((a>b) && (a>c) )then 
		maior := a;
      else
         if (b+c>10) then
             maior := b;
         else
			maior := c;
 		 end;
end;
      write(maior);
   }